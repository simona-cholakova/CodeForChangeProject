/**
 * @fileoverview Main package entrypoint.
 * @author Nicholas C. Zakas
 */

"use strict";

const { name, version } = require("@eslint/js/package.json");

//------------------------------------------------------------------------------
// Public Interface
//------------------------------------------------------------------------------

module.exports = {
	meta: {
		name,
		version,
	},
	configs: {
		all: require("@eslint/js/src/configs/eslint-all"),
		recommended: require("@eslint/js/src/configs/eslint-recommended"),
	},
};
